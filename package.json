{
  "name": "aerogrow-backend",
  "version": "1.0.0",
  "description": "AeroGrow Hydroponics API Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
